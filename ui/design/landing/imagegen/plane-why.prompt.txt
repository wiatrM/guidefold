Generate a landing-page background image for Guidefold (a developer tool; this is brand artwork, purely visual). Use the native image_gen raster tool.
Concept: A near-black graphite field (#0F1418) seen from directly overhead, covered edge to edge in an even scatter of cream paper fragments (#E6E8EA) at low opacity, as if a dozen separate small maps were laid out on the same table at different angles and none of them agree. Each fragment carries a few fine contour lines. They overlap slightly and none dominates. A faint dashed survey grid (#677681, very dim) runs beneath everything. One thin teal contour ring (#2BA6A0) crosses the lower third. No orange. Even, flat, diffuse light with no hotspot and no shadow direction. Very low contrast overall, matte, quiet, almost a texture rather than a scene.
Avoid (negative constraints): text, letters, numbers, watermark, logo, the letter G, UI, screens, hands, people, orange, red, blue, purple, neon, glow, bloom, lens flare, strong highlight, hotspot, hard shadow, high contrast, a single dominant object, centred composition, vignette, noise grain heavier than film base.
Absolutely no text, letters, numbers, logos, UI or people anywhere in the image.
Brand palette (hex): graphite #0F1418 background, deep graphite #081014, stone cream paper #E6E8EA, steel #677681, survey teal #2BA6A0, safety orange #FF6A28 (accent only where the concept says).
Generate at 1536x1024 landscape. Then post-process with ffmpeg to exactly 1920x1080: crop the 3:2 output to 16:9 keeping the full width (centred vertical crop), then scale to 1920x1080, and save as PNG at the relative path public/assets/landing/plane-why.png (create the directory if needed). Do not write any other files. In your final answer state explicitly whether image_gen was used and the final path and dimensions (verify with ffprobe).