Generate a landing-page background image for Guidefold (a developer tool; this is brand artwork, purely visual). Use the native image_gen raster tool.
Concept: Overhead three-quarter view of a surveyor's table in near-darkness. The left 55% of the frame is an almost empty deep graphite field (#0F1418) with only the faintest dashed survey grid and one or two very dim teal contour rings (#2BA6A0), nothing brighter than a whisper. The right 40% holds a single large cream folded paper map (#E6E8EA) with fine printed topographic contour lines, creased along three visible fold seams so it stands in low relief like a ridge, its highest fold apex sitting slightly right of centre and just above the vertical middle. A single continuous orange route line (#FF6A28), 3px equivalent, travels across the paper from its lower left toward the upper right and ends in a small circular waypoint. Cool low-angle light from the upper right, soft falloff into the left of the frame, matte paper, no gloss. Flat technical illustration crossed with a still-life photograph: precise, calm, engineering. Palette limited to #0F1418, #081014, #E6E8EA, #677681, #2BA6A0 and one accent of #FF6A28.
Avoid (negative constraints): text, letters, numbers, typography, watermark, logo, brand mark, the letter G, user interface, screens, buttons, charts, hands, people, faces, blue, purple, magenta, neon, glow, lens flare, bloom, heavy vignette, bokeh, glassmorphism, gradient mesh, 3D render sheen, plastic, clutter on the left side of the frame, bright objects in the left half, multiple orange lines, orange used as ambient colour, busy texture behind the copy area.
Absolutely no text, letters, numbers, logos, UI or people anywhere in the image.
Brand palette (hex): graphite #0F1418 background, deep graphite #081014, stone cream paper #E6E8EA, steel #677681, survey teal #2BA6A0, safety orange #FF6A28 (accent only where the concept says).
Generate at 1536x1024 landscape. Then post-process with ffmpeg to exactly 1920x1080: crop the 3:2 output to 16:9 keeping the full width (centred vertical crop), then scale to 1920x1080, and save as PNG at the relative path public/assets/landing/hero-keyframe.png (create the directory if needed). Do not write any other files. In your final answer state explicitly whether image_gen was used and the final path and dimensions (verify with ffprobe).