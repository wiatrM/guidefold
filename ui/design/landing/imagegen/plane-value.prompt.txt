Generate a landing-page background image for Guidefold (a developer tool; this is brand artwork, purely visual). Use the native image_gen raster tool.
Concept: A near-black graphite field (#0F1418) seen from directly overhead. Three separate cream paper maps (#E6E8EA) of similar size lie side by side across the frame, evenly spaced, each folded once and printed with fine contour lines, each turned to a slightly different angle. A faint dashed survey grid (#677681, very dim) runs beneath all three. One thin continuous orange route line (#FF6A28) passes across all three sheets from the left edge to the right edge, touching each one, with a small circular waypoint on each sheet. Even flat diffuse light, no hotspot, very low contrast, matte.
Avoid (negative constraints): text, letters, numbers, watermark, logo, the letter G, UI, screens, hands, people, red, blue, purple, neon, glow, bloom, lens flare, strong highlight, hard shadow, high contrast, more than one orange line, orange used as fill or ambient colour, vignette, 3D render sheen.
Absolutely no text, letters, numbers, logos, UI or people anywhere in the image.
Brand palette (hex): graphite #0F1418 background, deep graphite #081014, stone cream paper #E6E8EA, steel #677681, survey teal #2BA6A0, safety orange #FF6A28 (accent only where the concept says).
Generate at 1536x1024 landscape. Then post-process with ffmpeg to exactly 1920x1080: crop the 3:2 output to 16:9 keeping the full width (centred vertical crop), then scale to 1920x1080, and save as PNG at the relative path public/assets/landing/plane-value.png (create the directory if needed). Do not write any other files. In your final answer state explicitly whether image_gen was used and the final path and dimensions (verify with ffprobe).