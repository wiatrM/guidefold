import type {ComponentProps} from 'react';
import {RouteState} from './index';
export default {title:'Guidefold/RouteState',component:RouteState};
export const Default={args:{state:'empty',title:'No observations',description:'No adapter events or outcome assessments are available. Usefulness is Unknown.'} satisfies ComponentProps<typeof RouteState>};
