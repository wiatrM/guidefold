import {chromium} from '@playwright/test';const b=await chromium.launch(),p=await b.newPage({viewport:{width:1280,height:720}});await p.goto('http://127.0.0.1:4330/library',{waitUntil:'networkidle'});await p.locator('main [aria-busy=true]').waitFor({state:'hidden'});console.log(await p.locator('main h1, main section, #library-filters, #library-filters>div, main caption, main thead, tbody tr:first-child').evaluateAll(es=>es.map(e=>({tag:e.tagName,cls:e.className,text:e.textContent.slice(0,80),y:e.getBoundingClientRect().y,height:e.getBoundingClientRect().height,cols:getComputedStyle(e).gridTemplateColumns}))));await b.close();