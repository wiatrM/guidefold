import {chromium} from '@playwright/test';const b=await chromium.launch();const p=await b.newPage();p.on('console',m=>console.log(m.type(),m.text()));p.on('pageerror',e=>console.log('ERROR',e.message));await p.goto('http://127.0.0.1:4330/proposals',{waitUntil:'networkidle'});console.log('initial',(await p.locator('body').innerText()).slice(-1000));await p.getByRole('heading',{name:'Source to candidate',exact:true}).waitFor({timeout:20000});console.log('loaded',await p.locator('body').innerText());await b.close();