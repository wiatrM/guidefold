---
name: ui-ux-pro-max
description: Query a pinned UI/UX design database and turn the results into an evidence-backed design system. Use for every generated or redesigned page during shaping, implementation, repair, and read-only review.
---

# UI UX Pro Max

Use the bundled database and Python search tool. Do not install the global CLI, fetch updates, or use the network during a job.

## Resource routing

- Load [the repository workflow](./resources/workflow.md) before querying or applying recommendations.
- Use the bundled `scripts/search.py` and `data/` files only through that workflow.

The preservation contract and sanitized source evidence outrank database recommendations. This skill advises the chosen writer; it never creates a second writer or edits the site during review.
