interface:
  display_name: "Cinematic Landing"
  short_description: "Compose sparse film-led landing narratives"
  default_prompt: "Use $cloudfloo-cinematic-landing to shape this approved brief into a sparse cinematic chapter composition."
policy:
  allow_implicit_invocation: false
